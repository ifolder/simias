/**
 * $RCSfile: ActionsView.js,v $
 * 
 * @fileoverview iFolder.ActionsView Class
 */

/**
 * @class A class for the actions view.
 */
iFolder.ActionsView = Class.create();

/**
 * Construct a new iFolder.ActionsView object.
 * @constructor
 */
iFolder.ActionsView.prototype.initialize = function(store, parentNode)
{
	this.store = store;
	this.parentNode = parentNode;
};

/**
 * Render the view.
 */
iFolder.ActionsView.prototype.render = function()
{
};

