/**
 * $RCSfile: iFolderNewView.js,v $
 * 
 * @fileoverview iFolder.iFolderNewView Class
 */

/**
 * @class A class for the entries view.
 */
iFolder.iFolderNewView = Class.create();

/**
 * Construct a new iFolder.iFolderNewView object.
 * @constructor
 */
iFolder.iFolderNewView.prototype.initialize = function(store, parentNode)
{
	this.store = store;
	this.parentNode = parentNode;
};

/**
 * Render the view.
 */
iFolder.iFolderNewView.prototype.render = function()
{
};

