/**
 * $RCSfile: iFolderDetailsView.js,v $
 * 
 * @fileoverview iFolder.iFolderDetailsView Class
 */

/**
 * @class A class for the entries view.
 */
iFolder.iFolderDetailsView = Class.create();

/**
 * Construct a new iFolder.iFolderDetailsView object.
 * @constructor
 */
iFolder.iFolderDetailsView.prototype.initialize = function(store, parentNode)
{
	this.store = store;
	this.parentNode = parentNode;
};

/**
 * Render the view.
 */
iFolder.iFolderDetailsView.prototype.render = function()
{
};

