/**
 * $RCSfile: FileUploadView.js,v $
 * 
 * @fileoverview iFolder.FileUploadView Class
 */

/**
 * @class A class for the entries view.
 */
iFolder.FileUploadView = Class.create();

/**
 * Construct a new iFolder.FileUploadView object.
 * @constructor
 */
iFolder.FileUploadView.prototype.initialize = function(store, parentNode)
{
	this.store = store;
	this.parentNode = parentNode;
};

/**
 * Render the view.
 */
iFolder.FileUploadView.prototype.render = function()
{
};

