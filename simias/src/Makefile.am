DIST_SUBDIRS = core server webservices webaccess admin setup

if SIMIAS_AS_CLIENT
SUBDIRS = core
else
SUBDIRS = $(DIST_SUBDIRS)
endif


EXTRA_DIST = 

install-exec-local:

uninstall-local:

installcheck-summary:

clean-local:

distclean-local:

maintainer-clean-local:

