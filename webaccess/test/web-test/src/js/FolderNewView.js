/**
 * $RCSfile: FolderNewView.js,v $
 * 
 * @fileoverview iFolder.FolderNewView Class
 */

/**
 * @class A class for the entries view.
 */
iFolder.FolderNewView = Class.create();

/**
 * Construct a new iFolder.FolderNewView object.
 * @constructor
 */
iFolder.FolderNewView.prototype.initialize = function(store, parentNode)
{
	this.store = store;
	this.parentNode = parentNode;
};

/**
 * Render the view.
 */
iFolder.FolderNewView.prototype.render = function()
{
};

