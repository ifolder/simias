/**
 * $RCSfile: FolderDetailsView.js,v $
 * 
 * @fileoverview iFolder.FileDetailsView Class
 */

/**
 * @class A class for the entries view.
 */
iFolder.FileDetailsView = Class.create();

/**
 * Construct a new iFolder.FileDetailsView object.
 * @constructor
 */
iFolder.FileDetailsView.prototype.initialize = function(store, parentNode)
{
	this.store = store;
	this.parentNode = parentNode;
};

/**
 * Render the view.
 */
iFolder.FileDetailsView.prototype.render = function()
{
};

